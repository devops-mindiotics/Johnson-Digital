import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ClassesPage() {
  return (
    <Card>
        <CardHeader>
            <CardTitle>Class & Sections Management</CardTitle>
        </CardHeader>
        <CardContent>
            <p>This page is for Admins to configure classes, sections, and license distribution.</p>
        </CardContent>
    </Card>
  );
}
