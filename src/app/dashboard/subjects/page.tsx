import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function SubjectsPage() {
  return (
    <Card>
        <CardHeader>
            <CardTitle>Subjects</CardTitle>
        </CardHeader>
        <CardContent>
            <p>This page is for Students to access their subjects, lessons, and learning materials.</p>
        </CardContent>
    </Card>
  );
}
