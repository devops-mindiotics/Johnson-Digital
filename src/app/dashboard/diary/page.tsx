import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function DiaryPage() {
  return (
    <Card>
        <CardHeader>
            <CardTitle>Diary</CardTitle>
        </CardHeader>
        <CardContent>
            <p>The digital diary for notes, events, and communication will be available here.</p>
        </CardContent>
    </Card>
  );
}
